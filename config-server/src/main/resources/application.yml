server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://${user.dir}/config-repo
          clone-on-start: true
          default-label: main

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
